# GrokBuild adapter

The GrokBuild adapter owns provider command flags, session semantics,
structured result parsing, cancellation, and capability probing. Provider-native
background or multi-agent features are optional optimizations behind the common
V6 protocol.
