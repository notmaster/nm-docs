# Write a project Spec

Use discovery records to propose an immutable project Spec with stable Goal,
Requirement, Acceptance, and Decision IDs. Every mandatory Acceptance criterion
must declare `required_by_stage`. Do not confirm the Spec or write runtime state;
request trusted administrator confirmation through the V6 CLI.
