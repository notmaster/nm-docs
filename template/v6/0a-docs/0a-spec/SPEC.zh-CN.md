---
spec_id: SPEC-EXAMPLE-V1
document_title: 示例项目 Spec
status: draft
version: 1
workflow: v6
language: zh-CN
normative: false
execution_source: SPEC.md
implementation_authorized: false
---

# 示例项目 Spec

## 结果

请用项目请求的结果、约束、假设、风险、成功条件、失败条件、安全约束和非目标替换此草稿。在 `traceability.json` 中保持稳定的 Goal、Requirement、Acceptance、Decision、Phase 和 Task 标识。

## 交付

这个完整假 fixture 把发布与部署标为必需。真实项目在确认前必须明确声明每个交付阶段为 required 或 `not_applicable`。

## 确认边界

修改 `status` 只是评审提示。`SPEC_GATE` 需要单独签名的受信 confirmation record，并绑定此文档的 canonical hash。
