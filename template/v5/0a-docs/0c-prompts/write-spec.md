# Prompt: Write Spec (optional helper)

Write an NM V5 Spec in **English** using `0c-workflow/SPEC_TEMPLATE.md`.

Include problem, goals, non-goals, constraints, testable acceptance criteria,
design notes, optional phases, risks, and open questions.

Do not set `status: confirmed` unless the administrator explicitly confirms.
